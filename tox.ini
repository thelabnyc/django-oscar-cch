[tox]
isolated_build = True
toxworkdir={env:TOX_WORK_DIR:.tox}
envlist =
    py{312,313,314}-django{420}-oscar{42}
    py{312,313,314}-django{520}-oscar{42}

[testenv]
runner = uv-venv-runner
deps =
    django420: django>=4.2,<4.3
    django520: django>=5.2,<5.3
    oscar42: django-oscar>=4.2,<4.3
setenv =
    PYTHONWARNINGS = d
    DJANGO_SETTINGS_MODULE = sandbox.settings
    PYTHONPATH = {toxinidir}
changedir = {toxinidir}
commands =
    mypy oscarcch/ sandbox/
    coverage erase
    coverage run --source oscarcch -p manage.py test oscarcch --noinput
    - coverage combine
    - coverage report -m
